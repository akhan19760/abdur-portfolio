// Shared design-system primitives (buttons, typography, feedback, inputs).
// Barrel re-exports get added here as each primitive is built —
// see the component-design skill for conventions.
export {}
